# Remote Docker Configuration for Testcontainers
# This configuration uses the test machine to create temporary test containers
# Safe for destructive tests - isolated from development environment
#
# To use a custom test machine:
# export TEST_MACHINE_IP=192.168.2.100
# export $(cat .env.remote-docker | xargs)

# Enable remote Docker usage
USE_REMOTE_DOCKER=true

# Remote Docker host URL (default: 192.168.2.202:2375)
# Note: This uses a hardcoded IP. For dynamic configuration, use:
# export REMOTE_DOCKER_HOST=tcp://${TEST_MACHINE_IP}:2375
REMOTE_DOCKER_HOST=tcp://192.168.2.202:2375

# Optionally disable Ryuk (not recommended unless experiencing issues)
# DISABLE_RYUK=true

# Testcontainers specific settings
# TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock
# TESTCONTAINERS_HOST_OVERRIDE=192.168.2.202

# Note: When using remote Docker, testcontainers will:
# 1. Create containers on the remote host (192.168.2.202)
# 2. Automatically manage container lifecycle
# 3. Clean up containers after tests (via Ryuk or explicit stop)